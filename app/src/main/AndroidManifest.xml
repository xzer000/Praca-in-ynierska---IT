<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="adrianbrociek.praca_inzynierksa"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />

    <android:uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- START -->
        <activity
            android:name=".start"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MENU -->
        <activity
            android:name=".menu"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >

        </activity>

        <!-- WYKRESY -->
        <activity
            android:name=".wykresy"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".odczyt"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_odczyt" >
        </activity>
        <activity
            android:name=".sterowanie"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_sterowanie" >
        </activity>
        <activity
            android:name=".ustawienia"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_ustawienia" >
        </activity>
    </application>

</manifest>